# 法人サイト（alchemy-net） 開発進捗状況

## 現在地
- 現フェーズ: **完成・公開中**。⚖️356 ①⭕ ②⭕ ③⭕ で、**残作業は無い**（残 0人日）。
- 状態: 🔬 2026-09-12 実測 — `https://alchemy-net.com/` ほか **5ページすべて 200**（0.22〜0.50秒）、`www` 付きも 200。配信は **GitHub Pages**（`server: GitHub.com`・`status: built`・`main` の `/` を直配信・CNAME `alchemy-net.com`）。最後の変更は 板#57（賃管士アプリのサポートページ）で、その**受入条件を本日もう一度撃ち直して合格を確認**した。
- 次の一手: **無い**。⛔ 新しい依頼が来るまで、このリポは触らない（刹那性）。

## 1. 基本情報

- **ステータス**: **完成・本番公開中**（GitHub Pages・独自ドメイン `alchemy-net.com`）
- **完了タスク数**: **11/11**（⛔ ただし 6フェーズは「静的5ページの器には該当しない」ことを実測して確定させたもの。下表を参照）
- **進捗率**: **100%**／⚖️356 の完成基準では **①⭕ ②⭕ ③⭕**
- **次のマイルストーン**: 無し（完成済み）
- **最終更新日**: 2026-09-12
- **残人日**: 🎯 **0人日・完成済み**
  - 出典: `司令室/日直/実測-残人日-その他7本-20260911.md` §4
  - ⛔ **測れないもの**: 無し

### 🚨 ⚖️356 の3条件

| 条件 | 判定 | 🔬 何を撃って何が返ったか |
|---|:--:|---|
| ① 動いているか | ⭕ | 5ページすべて 200（`/` 0.50秒／`business.html` 0.25秒／`company.html` 0.22秒／`privacy.html` 0.22秒／`support/chinkanshi.html` 0.25秒）。`www.alchemy-net.com` も 200。⛔ 陰性対照: 存在しない `/support/no-such-page.html` は **404**＝200 は本物 |
| ② 主君がお使いになれるか | ⭕ | 各ページの `<title>` が本番に出ている（`ホーム｜アルケミー株式会社`／`事業内容｜`／`会社概要｜`／`プライバシーポリシー｜`／`サポート｜賃管士 条文で解く｜`）＝**中身まで届いている** |
| ③ 会社の金になっているか | ⭕ | 板#57 の受入条件を本日撃ち直して合格 — `support/chinkanshi.html` が **200**・`sales@alchemy-net.com` **2件**・「賃管士」**6件**。⇒ **App Store 審査に要るサポートページとして実際に機能している** |

---

## 2. 実装計画

### 開発フェーズ

> 🚨 **`[x]` は実測で裏を取ったものだけ。**
> 🚨 **このアプリは「HTML 5枚 ＋ CSS 1枚（合計 403行）」の静的サイトである。**⛔ だからといってフェーズを「該当なし」の一言で済ませず、**何をもってそう判断したかを毎行に書いた**。

| フェーズ | 状態 | 解説（🔬 何を撃って何が返ったか） |
|---------|:----:|------|
| **Phase 1: 要件定義** | [x] | 🔬 **要件書はリポの外にある**（`git ls-files` = **7ファイル**で `docs/` は本便まで存在しなかった）。正本は司令室の裁定台帳 — **裁定163**（`当直ログ-2026-08-25.md`・🗣 主君「法人サイトからアクセスできませんか」→「そのままでお願いします」）と **板#57**。⇒ 要件は定義されており、置き場が司令室なだけ |
| **Phase 2: Git・GitHub管理** | [x] | 🔬 `kikuosasaki1963-netizen/alchemy-corporate-site`・既定ブランチ `main`・PR は **#1** まで。コミット4本（サイト本体 → CNAME 2回 → 板#57 のサポートページ）。⛔ **CI も git hook も無い**（`.github/` `.githooks/` のどちらも存在しない）＝**品質の門は「人がレンダリングして見る」しかない**。⇒ ⛔ このリポで「CI 緑」「hook PASS」と書いてはいけない |
| **Phase 3: フロントエンド基盤** | [x] | 🔬 **素の HTML + CSS**。`style.css` **58行**を5ページが共有し、`<header class="site">` / `<nav class="site">` / `.wrap` / footer の型でそろっている。⛔ フレームワーク・ビルド器は**使っていない**（`package.json` も `*.config.*` も存在しない）＝**入れなかったのが設計**であって、未整備ではない |
| **Phase 4: ページ実装** | [x] | 🔬 **5ページすべて実装済み・本番で 200**: `index.html`(70行) ホーム／`business.html`(56行) 事業内容／`company.html`(61行) 会社概要／`privacy.html`(78行) プライバシーポリシー／`support/chinkanshi.html`(80行) 賃管士アプリのサポート |
| **Phase 5: 環境構築** | [x] | 🔬 GitHub Pages の設定を API で確認 — `status: built`・`build_type: legacy`・`source: {branch: main, path: /}`・`cname: alchemy-net.com`。独自ドメインは `CNAME` ファイル1行で効いており、`https` も `www` も通っている。⛔ 環境変数・秘密鍵は**1つも要らない**（下の Phase 6 の根拠と同じ） |
| **Phase 6: バックエンド計画** | [x] | ⛔ **バックエンドを持たない器であることを実測で確定させた**（「該当なし」で済ませていない）。🔬 全5ページ＋CSS に対して: `<script` **0件**／`<form` **0件**／`fetch(`・`XMLHttpRequest`・`axios` **0件**。外部を指す URL は **1本だけ**で、それも `<a href="https://app.alchemy-net.com/privacy.html">` という**ただのリンク**（別アプリのプライバシーポリシーへ飛ぶだけ・200 で生存）。⇒ **サーバ側の処理が入り込む余地が構造的に無い**ので、バックエンドの計画は要らない |
| **Phase 7: バックエンド実装** | [x] | ⛔ 同上。Phase 6 で測ったとおり**実装すべきバックエンドが存在しない**。⇒ 未実装ではなく**不要**。⚠️ 将来フォームや問い合わせ送信を足すなら、この行から作り直すことになる（いまは `sales@alchemy-net.com` へのメール導線だけで足りている） |
| **Phase 8: API統合** | [x] | ⛔ 同上。`fetch` 0件＝**呼ぶ API が1本も無い**。⇒ 統合すべきものが無い |
| **Phase 9: E2Eテスト** | [x] | 🔬 **自動テストの器は無い**が、**受入条件そのものが機械で撃てる形で板に書かれており、本日それを撃って合格した**——板#57 の判定①「`/support/chinkanshi.html` が **200** で `sales@alchemy-net.com` と「賃管士」を含む」⇒ 実測 **200 / 2件 / 6件**。⛔ 陰性対照 `/support/no-such-page.html` = **404**（配車前の 404 が対照だった、という板の記録と同じ形で測れている）。⇒ この器で意味のある E2E は「5ページが 200 で、中の文字が届いているか」であり、それは満たしている |
| **Phase 10: ローカル動作確認** | [x] | 🔬 ビルド工程が存在しない（`package.json` なし）ため、**ローカルの産物と本番の産物は同一のファイルそのもの**。⇒ 確認は本番の実測で足りる（`last-modified: Mon, 24 Aug 2026 23:11:51 GMT` が板#57 の着地と一致）。⚠️ 当席は本便でサイトのファイルを1行も触っていない（足したのは `docs/SCOPE_PROGRESS.md` のみ） |
| **Phase 11: デプロイメント** | [x] | 🔬 **GitHub Pages・`main` 直配信**（`server: GitHub.com`・`x-github-edge-region: japaneast`・`via: 1.1 varnish`）。⇒ **`main` に merge した瞬間が deploy** であり、別のデプロイ工程は無い。🚨 **Vercel ではない**——配車票には「⑩⑪ は Vercel」とあったが、**応答ヘッダが `server: GitHub.com` を返した**ので現況は GitHub Pages である |

### 🔬 測り方（⛔ ここを外すと嘘の表になる）

| 対象 | ⭕ 正しい撃ち方 | ⛔ やってはいけない |
|---|---|---|
| ページの生死 | `curl -s -o /dev/null -w "%{http_code} %{time_total}"`。**そのうえで存在しないパスを撃って 404 が返ることを見る**（陰性対照） | 200 を1本撃って終わりにする。GitHub Pages は 404 ページも返すので、**対照が無いと「200 が本物か」が言えない** |
| 配信元 | **`curl -I` の `server` ヘッダを読む** | リポ名や過去の申し送りから推測する。🔬 **本件は「Vercel」という申し送りが現況と違っていた** |
| backend の不在 | **`<script` / `<form` / `fetch(` を全ファイルで数えて 0 件を示す** | 「静的サイトだから該当なし」と書く。⛔ **数えていないものは "無い" と言えない** |

### 🚨 いま残っていること

**無い。**🎯 残 **0人日・完成済み**（出典: `司令室/日直/実測-残人日-その他7本-20260911.md` §4）。

---

## 3. この進捗表が主君の VSCode に出るための条件（🔬 現物で確かめた）

| # | 条件 | 🔬 実測 |
|:-:|---|---|
| 1 | ファイル名と置き場が **`<プロジェクトのフォルダ>/docs/SCOPE_PROGRESS.md`** であること | ⭕ 本ファイル（`docs/` は本便で新設した） |
| 2 | 「ステータス／完了タスク数／進捗率／次のマイルストーン／最終更新日」は**拡張が計算しない**（ただの文字） | ⭕ §1 に6項目を文字として置いた |
| 3 | そのフォルダが BlueLamp のプロジェクト一覧（`~/.appgenius-ai/projects/projects.json`）に登録されていること | ⭕ **登録済み**。🚨 配車票には「BlueLamp 未登録」とあったが、**現況は登録されている**（登録名「**法人サイト（alchemy-net）**」・パス `~/VSC/alchemy-corporate-site`） |
